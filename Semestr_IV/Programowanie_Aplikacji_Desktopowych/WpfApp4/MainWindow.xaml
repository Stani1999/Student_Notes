<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Content="Filtruj według nazwy: " Grid.Column="0" Grid.Row="0" Margin="7" />
        <TextBox Name="Filtr" Grid.Row="0" Grid.Column="1" Margin="7" TextChanged="Filtr_TextChanged" />
        <Button Name="Usun" Grid.Row="1" Grid.Column="0" Margin="7" Content="Usuń" Click="Usun_Click" />
        <ListView x:Name="lvLista" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="7" MouseDoubleClick="lvLista_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Indeks" DisplayMemberBinding="{Binding indeks}"/>
                        <GridViewColumn Header="Nazwa" DisplayMemberBinding="{Binding nazwa}" />
                        <GridViewColumn Header="Ilość">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ilosc}" FontSize="13" Foreground="Red" FontWeight="ExtraBold" TextAlignment="Right" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Miasto" DisplayMemberBinding="{Binding miasto}" />
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
