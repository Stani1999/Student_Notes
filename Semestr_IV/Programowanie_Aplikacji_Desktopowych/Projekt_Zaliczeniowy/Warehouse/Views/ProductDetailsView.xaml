<UserControl x:Class="Warehouse.Views.ProductDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:types="clr-namespace:Warehouse.Models.Types"
             xmlns:uc="clr-namespace:Warehouse.UserControls">

<UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="types:Currency"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="unitsFromEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="types:Unit"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
        <Grid Background="White" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="300"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Content="&#x2190; Powrót do Listy" Command="{Binding ReturnToListCommand}" Width="140" HorizontalAlignment="Left" Background="#E0E0E0" FontWeight="SemiBold" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,25">
                <Button Content="Usuń z bazy" Command="{Binding DeleteCommand}" Visibility="{Binding CanDelete, Converter={StaticResource BooleanToVisibilityConverter}}" Background="#F44336" Foreground="White" FontWeight="Bold" Margin="0,0,10,0" Width="110"/>
                <Button Content="Dodaj następny" Command="{Binding SaveAndNextCommand}" Background="#2196F3" Foreground="White" FontWeight="Bold" Margin="0,0,10,0" Width="120"/>
                <Button Content="Zatwierdź" Command="{Binding SaveCommand}" Background="#4CAF50" Foreground="White" FontWeight="Bold" Width="100"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="Kod kreskowy" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="4" Text="Nazwa produktu" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="1" Grid.Column="6" Grid.ColumnSpan="2" Text="Aktualny stan" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="1" Grid.Column="8" Grid.ColumnSpan="2" Text="Zdjęcie Produktu" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,4"/>

            <TextBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding ProductBarcode, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,15"/>
            <TextBox Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="4" Text="{Binding ProductName, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,15"/>
            <TextBox Grid.Row="2" Grid.Column="6" Grid.ColumnSpan="2" Text="{Binding ProductQuantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,15"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Text="Wartość miary" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="1" Text="Jednostka" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="3" Grid.Column="3" Grid.ColumnSpan="5" Text="Grupa" FontWeight="SemiBold" Margin="0,0,10,4"/>

            <TextBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding MeasurandAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,15"/>
            <ComboBox Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="1" ItemsSource="{Binding Source={StaticResource unitsFromEnum}}" SelectedItem="{Binding SelectedUnit}" Margin="0,0,10,15"/>
            <uc:GroupSelectionControl Grid.Row="4" Grid.Column="3" Grid.ColumnSpan="5" DataContext="{Binding CategorySelector}" Margin="0,0,10,15"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Text="Cena" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="5" Grid.Column="2" Grid.ColumnSpan="1" Text="Waluta" FontWeight="SemiBold" Margin="0,0,10,4"/>
            <TextBlock Grid.Row="5" Grid.Column="3" Grid.ColumnSpan="5" Text="Kategoria" FontWeight="SemiBold" Margin="0,0,10,4"/>

            <TextBox Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding ProductPriceAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,15"/>
            <ComboBox Grid.Row="6" Grid.Column="2" Grid.ColumnSpan="1" ItemsSource="{Binding Source={StaticResource dataFromEnum}}" SelectedItem="{Binding SelectedCurrency}" Margin="0,0,10,15"/>
            <uc:CategorySelectionControl Grid.Row="6" Grid.Column="3" Grid.ColumnSpan="5" DataContext="{Binding CategorySelector}" Margin="0,0,10,15"/>

            <TextBlock Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="4" Text="Opis" FontWeight="SemiBold" Margin="0,20,10,4" VerticalAlignment="Bottom"/>
            <TextBlock Grid.Row="7" Grid.Column="4" Grid.ColumnSpan="4" Text="Rozpoznany tekst etykiety" FontWeight="SemiBold" Margin="0,20,10,4" VerticalAlignment="Bottom"/>

            <TextBox Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="4" 
                     Text="{Binding ProductDescription, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Visible" 
                     HorizontalScrollBarVisibility="Disabled"
                     VerticalContentAlignment="Top" 
                     Margin="0,0,10,0"/>

            <TextBox Grid.Row="8" Grid.Column="4" Grid.ColumnSpan="4" 
                     Text="{Binding ExtractedText, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Visible" 
                     HorizontalScrollBarVisibility="Disabled"
                     VerticalContentAlignment="Top" 
                     Background="#FAFAFA" 
                     Margin="0,0,10,0"/>

            <Grid Grid.Row="2" Grid.RowSpan="7" Grid.Column="8" Grid.ColumnSpan="2"
                  VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Height="267"  Grid.Row="0" BorderBrush="#DADADA" BorderThickness="1" Background="White" Padding="5" Margin="0,0,0,5">
                    <Image Source="{Binding ProductImageSource}" Stretch="Uniform"/>
                </Border>
                <Button Grid.Row="1" Content="Wybierz Zdjęcie" Command="{Binding SelectImageCommand}" Margin="0,0,0,10" Height="30"/>

                <TextBlock Grid.Row="2" Text="Zdjęcie Etykiety" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                <Border Height="267" Grid.Row="3" BorderBrush="#DADADA" BorderThickness="1" Background="White" Padding="5" Margin="0,0,0,10">
                    <Image Source="{Binding ProductLabelImageSource}" Stretch="Uniform"/>
                </Border>

                <Button Grid.Row="4" Content="Wybierz Etykietę" Command="{Binding SelectLabelImageCommand}" Margin="0,0,0,5" Height="30"/>
                <Button Grid.Row="5" Content="Skanuj etykietę" Command="{Binding ExtractTextFromLabelCommand}" Background="#2196F3" Foreground="White" FontWeight="Bold" Margin="0,0,0,5" Height="30"/>
                <Button Grid.Row="6" Content="Tłumacz na Opis" Command="{Binding TranslateLabelCommand}" Background="#FF9800" Foreground="White" FontWeight="Bold" Height="30"/>
            </Grid>

        </Grid>
    </ScrollViewer>
</UserControl>