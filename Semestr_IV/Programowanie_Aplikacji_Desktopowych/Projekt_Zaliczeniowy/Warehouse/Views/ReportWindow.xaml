<Window x:Class="Warehouse.Views.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Raport Magazynowy" Height="600" Width="650"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Miesięczny Raport Ruchu Magazynowego" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Rok:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,5,0" />
            <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="80" Padding="5" Margin="0,0,20,0" />

            <TextBlock Text="Miesiąc:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,5,0" />
            <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="60" Padding="5" Margin="0,0,20,0" />

            <Button Content="Generuj / Odśwież" Command="{Binding GenerateReportCommand}" Padding="15,5" Background="#2196F3" Foreground="White" FontWeight="Bold" />
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding ReportData}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserSortColumns="True"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produkt" Binding="{Binding ProductName}" Width="*" />
                <DataGridTextColumn Header="Przyjęto" Binding="{Binding Received}" Width="100" />
                <DataGridTextColumn Header="Wydano" Binding="{Binding Issued}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Zapisz do TXT" Command="{Binding SaveToFileCommand}" Background="#4CAF50" Foreground="White" FontWeight="Bold" Padding="20,10"/>
        </Grid>
    </Grid>
</Window>